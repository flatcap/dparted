Notes

dparted looks for *.so in ./plugin
	finds my_theme.so

dparted loads my_theme.so
	find symbol get_dparted_plugin_info
	info = call get_dparted_plugin_info

validate
	magic
	version
	function pointers

call my_theme::initialise (register info)
	my_theme calls register_theme ("My Theme", self)

