Container Properties

Types:
	int
	string
	bool

Actions:
	Read
	Write


vector<string>
get_prop_names (void)

get_prop (name, typed_recv)

get_int_ref (name)



GfxContainer:
	simplified interface
	read-only

	vector<string> get_prop_names();
	long           get_prop_long (name);
	string         get_prop_string (name);

Notes:
	get properties by type
		gpn (all)		// all properties
		gpn (read-write)	// properties that can be set
		gpn (properties)	// what the object wants to export


declare_prop (
	owner			Container
	name			bytes_size
	description		Size of the object in bytes
	get_prop()		get_bytes_size
	set_prop()		set_bytes_size
	flags			F_PROPERTY
)

int    get_prop_int    ("name");
double get_prop_double ("name");
string get_prop_string ("name");

void set_prop_int    ("name", value);
void set_prop_double ("name", value);
void set_prop_string ("name", value);


int    default_get_int    (PPtr)
double default_get_double (PPtr)
string default_get_string (PPtr)

void default_set_int    (PPtr)
void default_set_double (PPtr)
void default_set_string (PPtr)


int    custom_get_int    (PPtr)
double custom_get_double (PPtr)
string custom_get_string (PPtr)

void custom_set_int    (PPtr)
void custom_set_double (PPtr)
void custom_set_string (PPtr)


declare_prop (
	owner			Container
	variable&		bytes_size
	description		Size of the object in bytes
	flags			F_PROPERTY
)


std::string	name			read-write	default
std::string	name_default		read-only	dynamic
std::string	uuid			read-write	default
std::string	uuid_short		read-only	default
td::string	type			read-only	dynamic
std::string	type_full		read-only	dynamic

void*		address			read-only	dynamic	debug
void*		whole			read-only	default	debug
void*		parent			read-only	dynamic	debug	(weak ptr)
int		ref_count		read-only	dynamic	debug

long		block_size		read-only	default
long		bytes_size		read-only	default
long		bytes_used		read-only	default
long		bytes_free		read-only	dynamic		(calculate)
long		parent_offset		read-only	default

long		bytes_reserved?

magic suffixes:		// by flag?  F_GENERATE_SIZES
	kb,kib,mb,mib,gb,gib,tb,tib,pb,pib,eb,eib,human
on properties:
	bytes_*
	parent_offset

std::string	device			read-only	default
std::string	device_short		read-only	default
dev_t		device_major		read-only	default
dev_t		device_minor		read-only	default
std::string	device_major_minor	read-only	dynamic		(generate)


default behaviour
	get - copy variable
	set - copy value, inc seqnum

dynamic behaviour
	get - call function
	set - call function, inc seq num

