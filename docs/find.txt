Find

Small popup box in corner
Non-modal dialog

Options:
	match/whole
	case sensitive
	regex
	highlight all
	cumulative
		Add to previous matches
		Only in previous matches
	special: @property_name -> search here only
	maths/logic: @bytes_size < 100GiB

Show exact matches (whole field)
Show partial matches (partial field)
Show non-visible matches (in treeview, for example)
	[X] skip non-visible.  How?

