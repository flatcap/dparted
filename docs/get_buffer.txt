
map<void*,int>	ref count, size needed too & offset
wherever there's a fd keep track

get_buffer	if ((fd) & (in_range))
			dupe & inc rc
		if ((!fd) && (parent))
			delegate
put_buffer	if (fd)
			dec rc, delete on 0
		if ((!fd) && (parent))
			delegate
dtor		delete children

